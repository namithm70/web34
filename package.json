{
  "name": "defi-app-monorepo",
  "version": "1.0.0",
  "description": "Non-custodial DeFi web app with trading, staking, and farming",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "echo 'Build handled by Vercel'",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
